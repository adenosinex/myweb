 
# 🏕️ 摩托车露营装备清单

 
**营地实况天气**：{{ get_weather_text() }} 
 

### 气象数据看板
如果你想在 Markdown 里画表格，可以直接调取字典对象的字段：

| 地点 | 气温 | 相对湿度 | 风况 | 预测 |
|---|---|---|---|---|
| {{ get_weather_data()['data']['location'] }} | {{ get_weather_data()['data']['temperature'] }}°C | {{ get_weather_data()['data']['humidity'] }} | {{ get_weather_data()['data']['wind_direction'] }} {{ get_weather_data()['data']['wind_speed'] }} | {{ get_weather_data()['data']['forecast'] }} |

## 一、核心装备
* [x] 帐篷 (冷山2)
* [x] 睡袋 (-5度羽绒)
* [x] 防潮垫

## 二、机车车况数据看板
> 以下数据由系统接口动态拉取：

| 监控项 | 实时状态 | 备注 |
|---|---|---|
| 服务器CPU | **{{ sys_stats().cpu_usage }}** | 正常 |
| 内存占用 | **{{ sys_stats().memory_usage }}** | 正常 |

## 三、AI 分析图表
这里可以嵌入其他分析系统生成的在线图片链接，每次打开文档都会重新加载最新图片。
![动态折线图](https://via.placeholder.com/600x200.png?text=Live+Data+Chart)